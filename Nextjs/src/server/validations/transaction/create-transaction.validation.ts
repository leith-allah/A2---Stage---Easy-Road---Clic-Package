/*
import { z } from "zod";

export const createTransactionSchema =
  z.object({

    sourceWalletId:
      z.number(),

    destinationWalletId:
      z.number(),

    amount:
      z.number()
       .positive(),

    type:
      z.string(),

    status:
      z.string(),

  });
*/